<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farm.service.ReviewImgService">
	<!-- 이미지 등록 -->
	<insert id="insertImg"
		parameterType="com.farm.dto.ReviewImgDTO">
		INSERT INTO review_img
			(reviewimg_id, filename, idx, main, review_id)	
		VALUES
			(seq_reviewimg_id.nextval, #{filename} ,#{idx} , #{main} ,#{review_id})
	</insert>
	
	<!-- 이미지 조회 -->
	<select id="selectImg"
		resultType="com.farm.dto.ReviewImgDTO">
		SELECT reviewimg_id, filename, idx, main, review_id
			FROM review_img 
			WHERE review_id = #{review_id}
			ORDER BY review_id ASC
	</select>
	
	<!-- 이미지 삭제 -->
	<delete id="deleteImg"
		parameterType="com.farm.dto.ReviewImgDTO">
		DELETE FROM reviewimg_id WHERE review_id = #{review_id}	
	</delete>
</mapper>