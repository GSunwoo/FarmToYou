<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farm.service.IFarmService">
	
	<select id="selectFarmInfo" parameterType="Long" 
		resultType="com.farm.dto.FarmDTO">
		SELECT *
			FROM farm
				WHERE member_id = #{member_id}
	</select>
	
	<delete id="deleteFarm" parameterType="Long">
		DELETE FROM farm WHERE member_id = #{member_id}
	</delete>
	
	<insert id="insertFarm" parameterType="com.farm.dto.FarmDTO">
		INSERT INTO farm (farm_id, member_id, account, bank,
			brand_name, com_addr1, com_addr2, com_zip, depositor, owner_name)
		VALUES (#{farm_id}, #{member_id}, #{account}, #{bank}, 
			#{brand_name}, #{com_addr1}, #{com_addr2}, #{com_zip}, #{depositor}, #{owner_name})
	</insert>
</mapper>